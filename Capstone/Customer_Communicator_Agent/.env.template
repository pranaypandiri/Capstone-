# Customer Communicator Agent - Environment Configuration Template
# Copy this file to .env and fill in your values

# OpenAI API Configuration
OPENAI_API_KEY=your-api-key-here
OPENAI_MODEL=gpt-4o
OPENAI_BASE_URL=https://schoolofagenticaitraining.openai.azure.com/
OPENAI_API_TYPE=azure

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/agent.log

# Agent Configuration
AGENT_ID=COM-01
AGENT_NAME=Customer Communicator

# Message Settings
MESSAGE_TONE=empathetic
MESSAGE_MAX_LENGTH=500
INCLUDE_SIGNATURE=true

# Compliance Settings
CHECK_GDPR=true
CHECK_BRAND_TONE=true
CHECK_REGULATORY=true

# Dispatch Settings
DEFAULT_DISPATCH_CHANNEL=email
ENABLE_EMAIL=true
ENABLE_SMS=true
ENABLE_PORTAL=true

# Template Configuration
TEMPLATE_FILE=Data_Sources/communication_templates.json
DEFAULT_TEMPLATE=delay_resolution

# Performance Settings
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1000
LLM_CACHE_SEED=42

# Database Settings (for future use)
# DATABASE_URL=postgresql://user:password@localhost/o2c
# DB_CONNECTION_POOL_SIZE=10

# Email Dispatch Settings (for future use)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# SMS Dispatch Settings (for future use)
# SMS_PROVIDER=twilio
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token

# Monitoring & Observability
ENABLE_PROMETHEUS_METRICS=false
PROMETHEUS_PORT=8000
ENABLE_SENTRY_MONITORING=false
SENTRY_DSN=

# Development Settings
DEBUG_MODE=false
VERBOSE_LOGGING=false
DRY_RUN=false

# Workflow Settings
ORCHESTRATION_TIMEOUT=30
RETRY_ATTEMPTS=3
RETRY_DELAY_SECONDS=5

import json
import os
from datetime import datetime
from typing import Any, Dict, Optional
from dotenv import load_dotenv  # Add this
import autogen
from autogen import ConversableAgent, config_list_from_json

# Load environment variables from .env file
load_dotenv()  # Add this line
